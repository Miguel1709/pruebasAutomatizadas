from selenium.webdriver.common.by import By

class CartPageElements:
    """
    Define los localizadores (Targets) para los elementos en la Página del Carrito de Compras.
    """
    PROCEED_TO_CHECKOUT_BUTTON = (By.CLASS_NAME, 'check_out')